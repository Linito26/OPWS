# ===== Configuración de Entorno =====
# NODE_ENV: Entorno de ejecución (development, production, test)
NODE_ENV=production

# ===== Servidor =====
# PORT: Puerto en el que escuchará el servidor
PORT=2002

# ===== Base de Datos =====
# DATABASE_URL: URL de conexión a PostgreSQL
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# Ejemplo: postgresql://opws_user:secret_password@localhost:5432/opws_db?schema=public
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public

# ===== JWT (JSON Web Token) =====
# JWT_SECRET: Secreto para firmar los tokens JWT
# IMPORTANTE: Debe tener al menos 32 caracteres en producción
# NUNCA uses el valor por defecto en producción
# Genera uno seguro con: openssl rand -base64 48
JWT_SECRET=your-secure-jwt-secret-here-minimum-32-characters-required

# JWT_EXPIRES_IN: Tiempo de expiración del token JWT
# Ejemplos: 1h (1 hora), 7d (7 días), 30d (30 días)
JWT_EXPIRES_IN=7d

# ===== CORS =====
# FRONTEND_URL: URL del frontend para configurar CORS
# En producción, especifica la URL exacta de tu frontend
# Puedes especificar múltiples URLs separadas por comas
FRONTEND_URL=https://opws.yourdomain.com
# O múltiples:
# CORS_ORIGINS=https://opws.yourdomain.com,https://www.opws.yourdomain.com

# ===== Rate Limiting =====
# RATE_LIMIT_WINDOW_MS: Ventana de tiempo para el rate limiting global (en milisegundos)
# Por defecto: 900000 (15 minutos)
RATE_LIMIT_WINDOW_MS=900000

# RATE_LIMIT_MAX: Número máximo de requests por ventana de tiempo
# Por defecto: 100 requests cada 15 minutos
RATE_LIMIT_MAX=100

# ===== JSON Body Parser =====
# JSON_LIMIT: Tamaño máximo del payload JSON
# Por defecto: 10mb
JSON_LIMIT=10mb

# ===== Email (Nodemailer) =====
# Configuración para envío de emails (opcional, si usas email)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@opws.yourdomain.com

# ===== AWS SES (opcional) =====
# Si usas AWS SES para envío de emails
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_SES_FROM_EMAIL=noreply@opws.yourdomain.com

# ===== Logging =====
# LOG_LEVEL: Nivel de logging (error, warn, info, debug)
# LOG_LEVEL=info

# ===== The Things Network (TTN) =====
# Si usas The Things Network para IoT
# TTN_WEBHOOK_SECRET=your-ttn-webhook-secret
